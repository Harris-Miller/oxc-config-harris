// oxfmt doesn't yet support "extends", but putting this here for when that's added
const formatConfig = {
  arrowParens: 'avoid',
  printWidth: 120,
  singleQuote: true,
};

export default formatConfig;
